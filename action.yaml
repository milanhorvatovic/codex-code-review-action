name: "Codex AI Code Review"
description: "AI-powered code review using OpenAI Codex. Use the review and publish sub-actions."

branding:
  color: "blue"
  icon: "search"

runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        echo "::error::Do not use 'milanhorvatovic/codex-ai-code-review-action' directly. Please use the 'prepare', 'review', and 'publish' sub-actions. See the README for usage instructions."
        exit 1
